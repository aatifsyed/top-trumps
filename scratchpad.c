#include <stdio.h>
#include <stdlib.h>
#include <time.h> // for seeding RNG

int main()
{
    // srand(time(NULL));

    // for (int i = 0; i < 10; i++)
    // {
    //     printf("%d\n", ((int)rand()) % 255);
    // }  
    // return 0;

    int empty;
    printf("%d\n", empty);

    char a = '\0', b=-1;

    if (b > a)
    {
        printf("your life is slightly easier");
    }
}